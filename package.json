{
  "name": "@shiplohq/mcp",
  "version": "0.1.2",
  "description": "MCP server for the Shiplo deploy platform — deploy and manage static sites from your AI client",
  "license": "MIT",
  "main": "./dist/index.js",
  "bin": {
    "platform-mcp": "dist/cli.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "dev": "tsc && node dist/cli.js",
    "start": "node dist/cli.js",
    "test": "node --import tsx --test test/*.test.ts",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "@shiplohq/contracts": "^0.1.0",
    "@modelcontextprotocol/sdk": "^1.0.0",
    "sharp": "^0.34.5",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  },
  "engines": {
    "node": ">=24.0.0"
  },
  "publishConfig": {
    "access": "public"
  }
}
